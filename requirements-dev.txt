# RiceClipper — development dependencies.
-r requirements.txt

pytest==8.3.4
pytest-cov==7.1.0          # coverage-floor gate
httpx==0.28.1  # FastAPI TestClient
ruff==0.16.4               # lint + format gate (pinned so CI matches local)
pre-commit==4.6.2          # optional local hooks (.pre-commit-config.yaml)
websockets==17.1            # Chrome DevTools browser-layout checks
